# OrbitLauncher v0.2.2

本版本继续优化 Microsoft Surface Dial 的日常操作体验，并为 macOS 26 加入全新的 Liquid Glass 圆环界面。

## Dial 启动与滚动

- 新增“旋转启动”和“按下启动”两种圆环呼出方式。
- 使用“按下启动”时，圆环隐藏期间旋转 Dial 会直接模拟鼠标上下滚动；按下后呼出圆环，再旋转即可选择应用。
- 支持设置圆环隐藏时每格滚动 1–10 行，默认 3 行。
- 滚动方向自动适配 macOS“自然滚动”，逆时针向上、顺时针向下。
- 圆环显示后保持旋转选择、按下确认的原有操作方式。

## 分层精度与触觉反馈

- 第二层动作可以独立设置旋转精度，不再沿用第一层的固定精度。
- 切换层级时会立即刷新精度并清空残留旋转量，避免切层后误触发。
- “启用触觉反馈”作为旋转刻度的总开关，“切层时震动反馈”可独立启用。
- 第一层和第二层可分别设为“静音”或“咔哒”；静音层会关闭旋转刻度震动，咔哒层保留刻度反馈。
- 修复第一层设为静音后仍有震动的问题。

## macOS 26 Liquid Glass UI

- 圆环采用 Liquid Glass 材质，中间改为空心设计，并移除图标后的圆形底板。
- 缩小圆环和图标占用空间，使界面更轻巧。
- 新增圆环呼出位置设置，可选择跟随鼠标或固定在当前屏幕中央。
- 优化固定屏幕中央和冷启动时的预热流程，降低首次呼出的延迟。
- 增加分段延迟日志，便于继续分析设备回调、布局和窗口显示耗时。

## 快捷操作

- 快捷键设置新增 Control + Shift（⌃⇧）组合。
- 保留环形切换、逐应用直接控制和智能混合模式。

## 安装

1. 下载 `OrbitLauncher-macOS-arm64.zip` 并解压。
2. 将 `OrbitLauncher.app` 移动到“应用程序”文件夹。
3. 首次启动时按住 Control 点击应用并选择“打开”。
4. 在“系统设置 → 隐私与安全性 → 辅助功能”中允许 OrbitLauncher。

如果应用仍被 macOS 隔离，可执行：

```bash
xattr -dr com.apple.quarantine /Applications/OrbitLauncher.app
open /Applications/OrbitLauncher.app
```

## 已知限制

- 当前附件仅提供 Apple Silicon（ARM64）版本。
- 应用使用临时签名，没有 Apple Developer ID 公证。
- Liquid Glass 效果需要 macOS 26；较早系统使用兼容界面。
