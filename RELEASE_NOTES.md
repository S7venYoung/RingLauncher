# OrbitLauncher v0.1.0

首个可下载版本。OrbitLauncher 是一款 macOS 原生圆环应用启动器，面向鼠标滚轮、
映射为滚轮的旋钮编码器和键盘操作。

## 新功能

- 按全局快捷键在鼠标当前位置呼出圆环，快捷键可在设置中修改。
- 第一层显示当前正在运行的应用，并优先排列呼出前的前台应用。
- 可选第二层快捷操作，包含切换当前窗口、所有窗口、隐藏、退出和返回。
- 关闭第二层后，确认应用图标会直接切换或打开对应应用。
- 开启第二层后，默认操作固定为“切换当前窗口”，不会继承第一层的选择位置。
- 支持鼠标滚轮、映射成滚轮的编码器、方向键、回车和空格操作。
- 新增扇区高亮、径向拖动、出现与切换动画，以及浅色、深色和跟随系统外观。
- 可调整圆环尺寸、图标尺寸、滚轮步进、连续切换间隔和启动快捷键。
- 可开关应用与操作名称、切换音效、过渡动画和登录时启动。
- 以菜单栏应用运行，不占用 Dock。

## 历史问题修复

- 修复 OrbitLauncher 自己位于前台时无法接收滚轮事件的问题。
- 同时支持应用内与全局滚轮事件，兼容其他应用位于前台的使用场景。
- 修复编码器旋转一格却连续切换多项的问题。
- 修复滚轮设置未正确影响实际步进与连续切换速度的问题。
- 改进快速旋转识别，使用滚轮增量峰值识别连续物理格位。
- 修复快速旋转时因固定防抖间隔而漏掉格位的问题。
- 修复圆环被 AppKit 自动隐藏后遗留事件监听器，导致再次呼出时一次输入被处理三次的问题。
- 修复进入第二层时沿用第一层索引、默认操作看起来随机的问题。

## 安装

1. 下载 `OrbitLauncher-macOS-arm64.zip` 并解压。
2. 将 `OrbitLauncher.app` 移动到“应用程序”文件夹。
3. 首次启动时按住 Control 点击应用并选择“打开”。
4. 在“系统设置 → 隐私与安全性 → 输入监控”中允许 OrbitLauncher。

如果应用仍被 macOS 隔离，可执行：

```bash
xattr -dr com.apple.quarantine /Applications/OrbitLauncher.app
open /Applications/OrbitLauncher.app
```

## 已知限制

- 当前附件仅提供 Apple Silicon（ARM64）版本。
- 应用使用临时签名，没有 Apple Developer ID 公证。
- Microsoft Surface Dial 原始 HID 支持仍位于 `agent/surface-dial` 实验分支，本次 main
  Release 不包含该功能。
